var app = require('../app');
var engagement = app.models.engagement;
var signature = app.models.signature;

/*
engagement.prototype.add = function(n) {
	
	var sig = {};
  signature.find({
    where: {
      uuid: n.uuid,
		  major: nmajor,
			minor: nminor
    }
  }, function(err, signatures) {
		
		console.log( "signature found ");
		sig = signatures;		
    next();
  });
	
	//
	
  if(this.credit) {
    return n + this.credit;
  } else if(this.debit) {
    return n - this.debit;
  } else {
    return n;
  }
	
	
	
});


	if ( )
  if(this.credit) {
    return n + this.credit;
  } else if(this.debit) {
    return n - this.debit;
  } else {
    return n;
  }
}
*/

