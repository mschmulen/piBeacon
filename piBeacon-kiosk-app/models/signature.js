var app = require('../app');
var signature = app.models.signature;

/*
signature.prototype.add = function(n) {
	
	//if the signature already exists then just return id of the signature
	
	if ( )
  if(this.credit) {
    return n + this.credit;
  } else if(this.debit) {
    return n - this.debit;
  } else {
    return n;
  }
}
*/
