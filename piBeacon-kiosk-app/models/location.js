var app = require('../app');
var location = app.models.location;

/*
signature.prototype.add = function(n) {
	if ( )
  if(this.credit) {
    return n + this.credit;
  } else if(this.debit) {
    return n - this.debit;
  } else {
    return n;
  }
}
*/
